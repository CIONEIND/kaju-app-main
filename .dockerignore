Dockerfile
.dockerignore
node_modules
npm-debug.log
README.md
.next
.git

# Segredos — nunca devem entrar no contexto de build
/srv/kaju/dev/.env
/srv/kaju/prod/.env
.env
.env.*
!.env.example
config.ovpn
vpn-auth.txt

# Gerado dentro do builder pelo `prisma generate`
src/generated

# Ruído de build local / CI
*.tsbuildinfo
.gitlab-ci.yml
docker-compose*.yml
